# Written by Owen T. Parkins
# Only add these sources if the test application
# will be built

function(target_sources_test target)
	#	if(KILIB_BUILD_TESTS)
	#		target_sources(${target} ${ARGN})
	#	endif()
endfunction()
