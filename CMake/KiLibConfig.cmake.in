get_filename_component(KILIB_CMAKE_DIR "${CMAKE_CURRENT_LIST_FILE}" PATH)
include(CMakeFindDependencyMacro)

list(APPEND CMAKE_MODULE_PATH ${KILIB_CMAKE_DIR})

if(NOT TARGET KiLib::KiLib)
    include("${KILIB_CMAKE_DIR}/KiLibTargets.cmake")
endif()

set(KILIB_LIBRARIES KiLib::KiLib)