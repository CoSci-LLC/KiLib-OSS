include(ExternalProject)
ExternalProject_Add(stats
  GIT_REPOSITORY    https://github.com/kthohr/stats
  GIT_TAG           master
  CONFIGURE_COMMAND ""
  BUILD_COMMAND ""
  INSTALL_COMMAND ${CMAKE_COMMAND} -E copy_directory <SOURCE_DIR>/include/ ${EXTERNAL_INSTALL_LOCATION}/include/
)

add_dependencies(${projectName} stats)